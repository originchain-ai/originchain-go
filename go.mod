// Module is published under github.com/originchain/originchain-go via a
// mirror of this directory to a standalone repo (driven by the
// sdk-release-go.yml workflow). Tags on the standalone mirror follow
// SemVer (vX.Y.Z); the source of truth is this monorepo subdir.
//
// Pattern reference: kubernetes/client-go is a mirror of
// kubernetes/kubernetes/staging/src/k8s.io/client-go. Same shape.
module github.com/originchain-ai/originchain-go

go 1.21
